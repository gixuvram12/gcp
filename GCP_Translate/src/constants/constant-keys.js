const DATA_SAVED = 'Data have been added succesfully.';

const GCP = 'final';

const TRANSLATION = 'Dis bonjour à mon petit ami.';

const MSG_RECEIVED = 'Message(s) received.';

module.exports = { DATA_SAVED, GCP, TRANSLATION, MSG_RECEIVED };
